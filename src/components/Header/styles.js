const styles = theme => ({ 
    root: {
        fontSize: 16,
        width: '100%',
        height: 56,
        display: 'flex',
        justifyContent: 'center',
        border: [[1, 'solid', '#fafafa']],
        borderRadius: 8,
        boxShadow: [[0, 0, 8, 0, '#afafaf']]
    },

    logo: {
        maxWidth: 46,
        maxHeight: 46,
    }
})

export default styles